{% if valid_items|length > 0 %}
    {% include
        'NetgenMoreDemoBundle:parts/block_views:' ~ block.view ~ '.html.twig'
         with {
            'valid_items': valid_items,
            'block_type': 'content-grid',
            'block': block
        }
    %}
{% endif %}
