{{ render(
    controller(
        'ez_content:embedContent', {
            'contentId': content.id,
            'viewType': 'embed_inner',
            'layout': false,
            'params': {
                'objectParameters': objectParameters,
                'noLayout': true
            }
        }
    )
) }}
