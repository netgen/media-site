{% if valid_items|length > 0 %}
    {% include
        'NetgenMoreDemoBundle:blocks/content_grid:' ~ block.view ~ '.html.twig' ignore missing
         with {
            'valid_items': valid_items,
            'block_type': 'content-grid',
            'block': block
        }
    %}
{% endif %}
