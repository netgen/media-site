{% for valid_item in valid_items|slice( 0, 1 ) %}
    {{ render(
        controller(
            'ng_content:viewAction', {
                'contentId': valid_item.contentId,
                'locationId': valid_item.locationId,
                'viewType': 'slide'
            }
        )
    ) }}
{% endfor %}
