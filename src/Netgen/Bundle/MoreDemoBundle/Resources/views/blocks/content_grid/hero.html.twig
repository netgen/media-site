{% for valid_item in valid_items|slice( 0, 1 ) %}
    {{ render(
        controller(
            'ez_content:viewLocation', {
                'locationId': valid_item.locationId,
                'viewType': 'slide',
                'params': {
                    'alias_name': 'i480'
                }
            }
        )
    ) }}
{% endfor %}
