{{ render(
    controller(
        'ng_content:viewAction', {
            'content': item.object,
            'location': item.object.mainLocation,
            'viewType': 'gallery_thumb',
            'layout': false,
            'params': {
                'ngbm_item': item,
                'image_type': image_type,
                'show_details': show_details,
                'show_details_on_hover': show_details_on_hover
           }|merge(ezparams|default({}))
        }
    )
) }}
