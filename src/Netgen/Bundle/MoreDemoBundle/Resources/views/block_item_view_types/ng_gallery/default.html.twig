{# ng_gallery - default view type #}

{% set location_path = path( location ) %}

{% block content %}
    <div class="content-view-block-item view-type-{{ view_type|default( '' ) }} ng-gallery">
        <h2 class="att-title"><a href="{{ location_path }}">{{ ez_render_field( content, 'name' ) }}</a></h2>
    </div>
{% endblock %}
