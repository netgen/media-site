{% extends 'NetgenMoreBundle::pagelayout_resolver.html.twig' %}

{% trans_default_domain "ngmore_user" %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <div class="register-success">

                <div class="page-header">
                    <h1 class="title">{{ 'ngmore.user.register.success.title'|trans }}</h1>
                </div>

                {{ 'ngmore.user.register.success.text'|trans( { '%path%': path( 'login' ) } )|raw }}

            </div>
        </div>
    </div>
{% endblock %}