{{ render(
    controller(
        'ng_content:viewAction', {
            'content': content,
            'location': location,
            'viewType': 'ngcb_preview',
            'layout': false,
        }
    )
) }}
