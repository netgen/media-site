{{ render(
    controller(
        'ng_content:viewAction', {
            'content': item is defined ? item.content : content,
            'location': item is defined ? item.location : location,
            'viewType': 'ngcb_preview',
            'layout': false,
        }
    )
) }}
