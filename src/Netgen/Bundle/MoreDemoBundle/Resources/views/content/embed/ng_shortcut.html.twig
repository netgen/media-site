{% extends 'NetgenMoreDemoBundle:parts:linkable.html.twig' %}

{% block content %}
    <div class="view-type view-type-{{ viewType }} ng-shortcut">
        <h2 class="title"><a href="{{ url }}">{{ ez_render_field( content, 'title' ) }}</a></h2>
    </div>
{% endblock %}
