{% import 'NetgenMoreDemoBundle:parts:slide.html.twig' as slide %}

<div class="view-type view-type-{{ viewType }} ng-news vl2">
    {{ slide.image( content ) }}

    <div class="info">
        <div class="date">
            {{ ez_field_value( content, 'publish_date' ).value|date( 'j. n. Y.' ) }}
        </div>
        <h2 class="title"><a href={{ path( location ) }}>{{ ez_render_field( content, 'title' ) }}</a></h2>
    </div>
</div>
