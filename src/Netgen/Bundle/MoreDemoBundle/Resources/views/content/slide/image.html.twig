{% import '@NetgenMoreDemo/parts/slide.html.twig' as slide %}

<div class="view-type view-type-{{ viewType }} image vl2">
    {{ slide.image( content ) }}
    <div class="info">
        <h2 class="title"><a href={{ path( location ) }}>{{ content.fields.name.value.text }}</a></h2>
    </div>
</div>
