<div class="view-type view-type-{{ viewType }} image clearfix">
    {{ ez_render_field(
        content,
        'image', {
            parameters: {
                alias: 'i480',
                alt_text: ez_field_value( content, 'name' ).text
            }
        }
    ) }}
</div>
