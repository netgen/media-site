{# image - default view type #}

<div class="content-view-block-item view-type-{{ view_type|default( '' ) }} image">
    {{ ez_render_field(
        content,
        'image', {
            parameters: {
                alias: alias_name|default( 'original' ),
                alt_text: ez_content_name( content )
            }
        }
    ) }}
</div>
