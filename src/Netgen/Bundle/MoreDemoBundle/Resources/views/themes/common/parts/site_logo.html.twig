<a class="site-logo" href="{{ path(ezpublish.rootLocation) }}" title="{{ site_name }}">
    {% if not ngmore.siteInfoContent.fields.site_logo.empty %}
        <img src="{{ asset(ngmore.siteInfoContent.fields.site_logo.value.id, 'ezfile') }}" />
    {% else %}
        {{ site_name }}
    {% endif %}
</a>
