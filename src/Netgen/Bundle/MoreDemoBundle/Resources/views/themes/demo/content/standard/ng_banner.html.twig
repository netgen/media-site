{% extends '@ezdesign/parts/linkable.html.twig' %}

{% block content %}
<article class="view-type view-type-{{ viewType }} ng-banner vl1">

    {% if not content.fields.image.empty %}
        <figure class="image">
            {{ ng_render_field(
                content.fields.image, {
                    'parameters': {
                        'alias': 'i480',
                        'link_href': url,
                        'link_target': open_in_new_window ? '_blank' : ''
                    }
                }
            ) }}
        </figure>
    {% endif %}

    <header class="article-header">
	    <h2 class="title">
            <a href="{{ url }}" {% if open_in_new_window %} target="_blank" rel="noopener noreferrer" {% endif %}>
                {{ ng_render_field(content.fields.title) }}
            </a>
        </h2>
	</header>
</article>
{% endblock %}
