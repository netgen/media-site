{% extends '@ezdesign/parts/linkable.html.twig' %}

{% import '@ezdesign/parts/slide.html.twig' as slide %}

{% block content %}
    <div class="view-type view-type-{{ viewType }} ng-banner vl3 {{ content.fields.css_class.value.text|trim }}">
        {{ slide.image(content) }}

        <div class="info">
            <h2 class="title">
                <a href="{{ url }}" {% if open_in_new_window %} target="_blank" rel="noopener noreferrer" {% endif %}>
                    {{ ng_render_field(content.fields.title) }}
                </a>
            </h2>
        </div>
    </div>
{% endblock %}
