{{ render(
    controller(
        'ng_content:viewAction', {
            'contentId': item.object.id,
            'locationId': item.object.mainLocationId,
            'viewType': view_type,
            'layout': false,
            'params': {
                'ngbm_item': item,
            }|merge(ezparams|default({}))
        }
    )
) }}
