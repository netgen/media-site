{{ render(
    controller(
        'ng_content:viewAction', {
            'contentId': item.object.contentInfo.id,
            'locationId': item.object.id,
            'viewType': view_type,
            'layout': false,
            'params': {
                'ngbm_item': item,
            }
        }
    )
) }}
