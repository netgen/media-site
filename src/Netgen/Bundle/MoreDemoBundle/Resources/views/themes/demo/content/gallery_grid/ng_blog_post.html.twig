{% import '@ezdesign/parts/content_fields.html.twig' as content_fields %}

{% if not content.fields.image.empty %}
    <div class="view-type view-type-{{ viewType }} ng-blog-post vl2">
        {% if not content.fields.author_override.empty %}
            {% set author = content.fields.author_override.value %}
        {% else %}
            {% set author = content.owner.name %}
        {% endif %}

        {% set date = content.fields.publish_date.value.value|date('j. n. Y.') %}
        {% set title = content_fields.name(content) %}

        <a href="{{ asset(ng_image_alias(content.fields.image, 'i1200').uri) }}" title="{{ title }} by {{ author }} ({{ date }})" class="js-mfp-item">
            {{ ng_render_field(
                content.fields.image, {
                    parameters: {
                        alias: 'i1200',
                        alt_text: title
                    }
                }
            ) }}
        </a>
    </div>
{% endif %}
