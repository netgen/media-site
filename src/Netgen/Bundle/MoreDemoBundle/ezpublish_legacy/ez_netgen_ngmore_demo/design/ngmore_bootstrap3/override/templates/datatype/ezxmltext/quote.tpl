<div class="object-{if is_set($align)}{$align}{else}left{/if}">
    <blockquote>
        <div class="blockquote-body">{$content}</div>
        {if is_set( $author )}<small>{$author}</small>{/if}
    </blockquote>
</div>
