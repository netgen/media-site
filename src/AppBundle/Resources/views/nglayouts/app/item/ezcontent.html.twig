{% include '@ezdesign/parts/ng_view_content.html.twig' with {
    content: item.object.content,
    location: item.object.mainLocation,
    view_type: 'nglayouts_app_preview',
    params: { 'nglayouts_item': item }
} only %}
