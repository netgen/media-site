{% if ezpublish.configResolver.parameter('render_content_directly', 'ngsite') %}
    {{ ng_view_content(
        location is defined and location is not empty ? location : content,
        view_type, {
            'params': params|default([])
        }
    ) }}
{% else %}
    {{ render(
        controller(
            'ng_content:viewAction', {
                'content': content,
                'location': location,
                'viewType': view_type,
                'layout': false,
                'params': params|default([])
            }
        )
    ) }}
{% endif %}
