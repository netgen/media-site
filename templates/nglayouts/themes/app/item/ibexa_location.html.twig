{% include '@ibexadesign/parts/ng_view_content.html.twig' with {
    content: item.object.content,
    location: item.object,
    view_type: view_type,
    params: { 'nglayouts_item': item }|merge(ibexa_params|default({}))
} only %}
