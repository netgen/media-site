<a class="site-logo" href="{{ ibexa_path(ibexa.rootLocation) }}" title="{{ site_name }}">
    {% if not ngsite.siteInfoContent.fields.site_logo.empty %}
        <img src="{{ asset(ngsite.siteInfoContent.fields.site_logo.value.id, 'ibexa_file') }}" alt="{{ site_name }}" />
    {% else %}
        {{ site_name }}
    {% endif %}
</a>
