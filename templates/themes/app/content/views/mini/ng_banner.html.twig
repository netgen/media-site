{# content \Netgen\IbexaSiteApi\API\Values\Content #}
{# location \Netgen\IbexaSiteApi\API\Values\Location #}

{% import "@ibexadesign/content/macros/links.html.twig" as links %}

<article class="view-type view-type-{{ view_type }} ng-banner vl5">

    {% if not content.fields.image.empty %}
        <figure class="image">
            {{ links.image_link(content, 'image', 'i160') }}
        </figure>
    {% endif %}

    <header class="article-header">
        <h2 class="title">
            {{ links.content_link(content, content.fields.title.value.text) }}
        </h2>
    </header>
</article>
