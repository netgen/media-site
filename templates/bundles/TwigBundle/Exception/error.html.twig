{% include ibexa.configResolver.hasParameter('template.errors.' ~ status_code, 'ngsite') ?
    ibexa.configResolver.getParameter('template.errors.' ~ status_code, 'ngsite') :
    ibexa.configResolver.getParameter('template.errors.default', 'ngsite')
ignore missing %}
